name: Test Tenon

on:
  push:
    branches:
      - "**"
  workflow_dispatch:

jobs:
  core-and-oss-build:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install native build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake g++ libcurl4-openssl-dev

      - name: Run Rust and tenon-oss integration tests
        run: ./scripts/test.sh all
