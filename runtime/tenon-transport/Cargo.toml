[package]
name = "tenon-transport"
version = "0.1.0"
edition = "2021"

[dependencies]
prost = "0.14"
tokio = { version = "1.35", features = ["io-util", "net", "sync", "time"] }

[dev-dependencies]
tokio = { version = "1.35", features = ["macros", "rt", "io-util", "sync", "time"] }
